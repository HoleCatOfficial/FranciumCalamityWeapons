using CalamityMod;
using CalamityMod.CalPlayer;
using DestroyerTest.Content.Equips.PotionFlowers;
using Terraria;
using Terraria.ModLoader;

namespace FranciumCalamityWeapons.Common.Items
{
    /*
    public class DashGlobalItem : GlobalItem
    {
        public override bool InstancePerEntity => true;

        public int DjedCharm = ModContent.ItemType<DjedPillarCharm>();
        public int Lillies = ModContent.ItemType<LilliesOfImmortality>();

        public void DjedRamCompatibility(Player player)
        {
            CalamityPlayer modPlayer = player.Calamity();
            modPlayer.DashID = DjedRamDash.ID;
            player.dashType = 0;
            player.noKnockback = true;
        }

        public override void UpdateAccessory(Item item, Player player, bool hideVisual)
        {
            if (item.type == DjedCharm || item.type == Lillies)
            {
                DjedRamCompatibility(player);
            }
        }
    }\
    */
}